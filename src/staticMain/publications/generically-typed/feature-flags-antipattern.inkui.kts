include("base.inkui.part.kts")

markdownBlog(
    description = """
        Are feature flags always a good idea? Are they as useful as we think? 
        Or can feature flags actually be harmful to a project?
    """.trimIndent(),
    published = LocalDate(2024, 6, 14),
    keywords = listOf("feature flags", "anti-pattern", "software development"),
)
