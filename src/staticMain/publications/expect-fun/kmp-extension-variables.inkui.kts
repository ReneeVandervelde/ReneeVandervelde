include("base.inkui.part.kts")
useCodeBlocks = true

markdownBlog(
    description = """
        Quick tips for storing state as an extension variable on a class
        in Kotlin, including Kotlin Multiplatform.
    """.trimIndent(),
    published = LocalDate(2025, 9, 22),
    keywords = listOf("kotlin", "extensions", "stateful"),
)
