include("base.inkui.part.kts")

markdownBlog(
    description = """
        An independent audit of the ionspin kotlin multiplatform libsodium
        wrapper, verifying the library integrity and security through
        manual file review.
    """,
    published = LocalDate(2024, 7, 23),
    keywords = listOf("kotlin", "cryptography", "libsodium"),
)
